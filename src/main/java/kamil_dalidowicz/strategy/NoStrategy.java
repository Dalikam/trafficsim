package kamil_dalidowicz.strategy;

import kamil_dalidowicz.model.Intersection;

public class NoStrategy implements LightControlStrategy {
    @Override
    public void apply(Intersection intersection, long stepCount) {
    }
}